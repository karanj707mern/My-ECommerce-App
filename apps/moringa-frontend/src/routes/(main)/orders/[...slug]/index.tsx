import OrdersPage from "../../../../components/orders/OrdersPage";

// Optional catch-all for /orders/active, /orders/delivered, /orders/cancelled
// and /orders/support. The view is derived from the pathname inside OrdersPage.
export default OrdersPage;
