{
  "name": "@moringa/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared types and utilities between Moringa frontend and backend",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "lint": "eslint src/**/*.ts",
    "format": "prettier --write \"src/**/*.ts\""
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^6.0.0",
    "prettier": "^3.8.1"
  }
}
